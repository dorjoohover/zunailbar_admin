export enum TeamType {
  TEAM = 10,
  GROUP = 20,
  PARTNER = 30,
}

export enum ROLE {
  SYSTEM = 10,
  ADMIN = 20,
  MANAGER = 30,
  EMPLOYEE = 40,
  CLIENT = 50,
  ANY = 60,
}

export enum UserStatus {
  ACTIVE = 10,

  BANNED = 20,
}
