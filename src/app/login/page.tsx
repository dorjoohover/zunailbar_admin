import { LoginPage } from "./components";

const Page = async () => {
  return (
    <LoginPage
    // save={(token, branch, merchant) => handleLogin(token, branch, merchant)}
    />
  );
};

export default Page;
