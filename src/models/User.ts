export interface IUser {
  id: string;
  firstname: string;
  lastname: string;
  role: number;
  mobile: string;
  birthday: Date;
  added_by: string;
  branch_id: string;
  user_status: number;
  description: string;
  created_at?: Date;
}

// AJiltan, users
